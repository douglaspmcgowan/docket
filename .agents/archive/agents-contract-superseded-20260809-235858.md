# Sections removed from AGENTS.md when the managed block absorbed them

Removed from `C:\Users\dougl\Projects\docket\AGENTS.md`. Each heading below now lives inside the managed block, and the
text kept here is what the project carried outside it, which differed. Nothing here is
read by any tool; it exists so the removal is recoverable.

## Start and resume

1. Read this file, `TASK.md`, and recent `LOG.md`.
2. Run `git status --short --branch` and inspect worktrees.
3. Read `SPEC.md` for the feature contract, `MAP.md` for architecture and data flow, and `DESIGN.md` for interface rules.
4. Read `data-manifest.yaml` before external-data work.
